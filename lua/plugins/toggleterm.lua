return {
  "akinsho/toggleterm.nvim",
  version = "*",
  opts = {
    size = 40,
    --open_mapping = [[[<leader><C-t>]], -- same key LazyVim uses for its built‑in terminal
    shade_terminals = true,
    direction = "vertical", -- "horizontal", -- change to "vertical", "float", or "tab"
    start_in_insert = true,
  },
}
